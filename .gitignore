/server
/bin/
/internal/webui/dist/*
!/internal/webui/dist/index.html
/web/node_modules/
/web/out/
/data/
*.db
*.db-*

# Local secrets/config — .env.example is the tracked template.
.env

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
